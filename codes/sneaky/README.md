for image
    Image shud be present n same directory
 node index.js "Who is in this image?" --image silver.jpg --visible

--visible is to show the browser as it is working -> not mandatory

for plain text

node index.js "prompt" -v 
-v is to show the browser as it is working -> not mandatory

